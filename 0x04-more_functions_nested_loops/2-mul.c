#include "main.h"

/**
 *  * mul - a function that multiplies two integers
 *   * @a: the first number
 *    * @b: the second number
 *     * Return: the result
 */
int mul(int a, int b)
{
	return (a * b);
}
